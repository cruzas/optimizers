"""Test suite for og-optimizers.

Present so the suite is a proper package: without it, modules here are
importable under two different names (``conftest`` and ``tests.conftest``),
which mypy rejects.
"""
